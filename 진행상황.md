# Valorant Duo — 진행상황

**마지막 업데이트:** 2026년 7월 12일 (Google 로그인 + 보안 점검)

---

## 🎉 MVP 완성

실시간 듀오 매칭 사이트 **MVP 배포까지 완료**했습니다.

---

## 완성된 것

### Phase 0 — 프로젝트 기반 ✅
- [x] Next.js 16 + React 19 + TypeScript + Tailwind CSS 4 프로젝트 세팅
- [x] 발로란트 스타일 랜딩 페이지 UI
- [x] `.cursorrules` / `진행상황.md` 자동 기록
- [x] 한/영 언어 전환
- [x] Supabase 연결 + 보안 점검 + health API production 차단

### Phase 1 — 로그인 / 회원가입 ✅
- [x] 이메일 로그인·회원가입 (`/login`)
- [x] Google OAuth 로그인·회원가입 (`signInWithOAuth` + `/auth/callback`)
- [x] Navbar Login / Logout / 프로필 링크
- [x] 세션 유지 (`proxy.ts`)
- [x] `profiles` 테이블 + RLS (`001_profiles.sql`)
- [x] `/profile` 프로필 페이지

### Phase 2 — 계정 연동 ✅
- [x] **2-1 ~ 2-4** Riot / Discord / 전적(코드) 연동
- [x] **2-5** Riot / Discord 연동 해제 (`017_account_unlink.sql`)
- [x] **2-6** Valorant 전적 불러오기 (Production Key + `/api/valorant/sync`)
  - [x] 라이엇 연동 직후 자동 전적 동기화
  - [x] 프로필 「전적 불러오기」 버튼 → 최근 경기 목록 즉시 갱신
  - [x] 5분 쿨다운 + 에러 메시지 (한/영)
  - [x] Production Key 스모크 테스트 (`scripts/check-riot-key.mjs` → OK)
  - [x] 보안 패치: puuid 소유권 검증, 실패 시에도 쿨다운, link/sync 에러 메시지 살균, 전역 rate limit
  - [x] 한국 서버 샤드 수정 (`kr` 자동 감지 + `ap` 폴백) — 잘못된 `ap` 호출로 400/쿨다운 나던 문제 해결

### Phase 4 — 실시간 매칭 ✅
- [x] **4-1** 온라인 상태 (`007_online_presence.sql`)
- [x] **4-2** 매칭 큐 (`008_match_queue.sql`)
- [x] **4-3** 매칭 알고리즘 (`009_duo_matching.sql`)
- [x] **4-4** 파티 링크 + Discord 연결 UI (`010_match_connection.sql`)
  - [x] 보이스 선택 (발로란트 / Discord / No Voice)
  - [x] No Voice 불일치 시 취소 안내 + 상대 응답 대기 UI
  - [x] 파티 코드 + Discord 프로필 연결
  - [x] 보안 패치 (Discord ID 조건부 노출, 파티 코드 덮어쓰기 차단)
- [x] **4-5** 매칭 무응답 자동 취소 (`011_match_timeout.sql`)
  - [x] 상대 보이스 선택 항상 표시
  - [x] 90초 무응답 시 자동 취소 + 안내 메시지
- [x] **4-6** 매칭 중 이탈 처리 (`012_match_partner_offline.sql`)
  - [x] 상대 탭 닫기/이탈 시 남은 사람 → 기본 화면 + 「상대가 나갔습니다」
  - [x] 나갔다가 돌아온 사람 → 기본 화면 + 「매칭이 취소되었습니다」
- [x] **4-7** 파티 구성 완료/취소 + 게임 중 화면 (`013_match_setup_phase.sql`)
  - [x] 보이스 선택 후 완료/취소 버튼, 4분 타임아웃
  - [x] 양쪽 완료 → 「게임 중입니다」화면
- [x] **4-8** 매칭 전 참고사항 모달 (첫 이용 필수 + 1주일 다시 보지 않기)
- [x] **4-9** 매칭 후 리뷰 + 신뢰도/매너등급 (`015_match_reviews.sql`)
  - [x] 게임 종료 후 상대 평가 모달 (긍정/부정 태그, 나중에 하기 없음 — 필수)
  - [x] 신뢰도 0~100 + 매너등급 S/A/B/C/D
  - [x] review_count < 3 → 🆕 신규 유저 뱃지
  - [x] 매칭 카드·프로필에 등급/신뢰도/태그 노출
  - [x] RLS + RPC (참여한 match_id의 상대에게만 리뷰)
- [x] **4-10** 이탈 페널티 시스템 (`016_penalty_system.sql`)
  - [x] 매칭 잡혔는데 이탈 시 1~3회 경고 / 4회 5분 쿨다운 / 5회+ 15분 쿨다운
  - [x] 이탈마다 신뢰도 -8 감점
  - [x] 프로필 페이지에서 내 페널티 기록 전체 조회
  - [x] 쿨다운 중 큐 가입 시도 → 해제 시각 표시하며 차단
- [x] **4-11** No Voice 수락/거절 UX (`018_no_voice_flow.sql`)
  - [x] 상대 no voice 선택 시 「아니요, 괜찮습니다」 / 「매칭 취소」 버튼
  - [x] no voice 거절 시 페널티 없음
  - [x] no voice 고른 사람은 상대 수락 전 파티 구성 완료 버튼 숨김
- [x] **보안 패치** — CSRF(Origin) 검증, UUID 검증, expire RPC 스로틀, 큐 중복 차단, 보안 헤더
- [x] **보안 점검 (7/12)** — 전적 sync 쿨다운 우회 차단(최소 60초), 실패 시에도 타임스탬프 기록, OAuth 에러 문구 살균, 디버그 스크립트 git 제외

---

## 보안 상태 (2026-07-06)

| 항목 | 상태 |
|------|------|
| `.env` git 제외 | ✅ |
| service_role 서버 전용 | ✅ |
| RLS + RPC 직접 쓰기 차단 | ✅ |
| Open redirect 방지 | ✅ |
| POST Origin/CSRF 검증 | ✅ (패치) |
| matchId UUID 검증 | ✅ (패치) |
| Riot OAuth 소유 증명 | 🟡 Production Key/RSO 대기 |
| 분산 rate limit (Upstash) | 🟡 Vercel 인메모리 한계 |

### Phase 5 — 배포 ✅
- [x] `npm run build` 프로덕션 빌드 성공
- [x] `GET /api/health` smoke test
- [x] GitHub push + Vercel Import + 환경변수 설정
- [x] Supabase Redirect URL (배포 URL) 설정
- [x] 배포 URL에서 health / 로그인 / 매칭 확인 완료

---

## 유료 플랜 출시 때 💎

- [ ] Claude API 플레이스타일 분석
- [ ] AI 기반 매칭 가중치 (티어/역할)

---

## MVP 이후 (Riot Production Key)

- [x] 이용약관 `/terms` + 개인정보처리방침 `/privacy` (footer `Link`로 페이지 이동)
- [x] Production API Key 신청 → `riot.txt` 도메인 검증 **Verified 완료**
- [x] Production API Key → Valorant 전적 실제 동작 (로컬 `.env.local` + Vercel `RIOT_API_KEY`)
- [ ] Riot OAuth (RSO) — OP.GG 스타일 계정 연동
- [x] **문의 이메일** — `site.contact.ts` → leedoo101009@gmail.com

---

## 다음에 할 일 (선택)

- [ ] 프로필 → 라이엇 연동 → 「전적 불러오기」 실제 테스트 (최근 경기 있는 계정)
- [ ] Supabase SQL Editor에서 **`005_valorant_matches.sql`** 실행 (전적 테이블, 아직 미실행 시)
- [ ] Supabase SQL Editor에서 **`015_match_reviews.sql`** 실행 (리뷰 기능, 아직 미실행 시)
- [ ] Supabase SQL Editor에서 **`016_penalty_system.sql`** 실행 (페널티 기능, 아직 미실행 시)
- [ ] Supabase SQL Editor에서 **`017_account_unlink.sql`** 실행 (연동 해제)
- [ ] Supabase SQL Editor에서 **`018_no_voice_flow.sql`** 실행 (No Voice UX)
- [ ] Supabase SQL Editor에서 `011` ~ `014` migration 실행 (아직 안 했다면)
- [ ] 친구한테 배포 URL 공유해서 실제 2인 매칭 테스트
- [ ] Riot Production Key **승인 대기** (이메일/포털 확인)
- [ ] AI 분석 / 티어 매칭 (유료 플랜 기능)

---

## 현재 기술 스택 상태

| 항목 | 상태 |
|------|------|
| Supabase Auth | ✅ |
| Riot / Discord 연동 | ✅ |
| Valorant 전적 | ✅ Production Key 연동 |
| 실시간 매칭 | ✅ |
| Vercel 배포 | ✅ **완료** |
| Claude AI | 💎 유료 플랜 |
