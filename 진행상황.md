# Valorant Duo — 진행상황

**마지막 업데이트:** 2026년 7월 18일 (free 큐마다 규칙 재분석)

---

## 🎉 MVP 완성

실시간 듀오 매칭 사이트 **MVP 배포까지 완료**했습니다.

---

## 완성된 것

### Phase 0 — 프로젝트 기반 ✅
- [x] Next.js 16 + React 19 + TypeScript + Tailwind CSS 4 프로젝트 세팅
- [x] 발로란트 스타일 랜딩 페이지 UI
- [x] `.cursorrules` / `진행상황.md` 자동 기록
- [x] 한/영 언어 전환
- [x] Supabase 연결 + 보안 점검 + health API production 차단

### Phase 1 — 로그인 / 회원가입 ✅
- [x] 이메일 로그인·회원가입 (`/login`)
- [x] Google OAuth 로그인·회원가입 (`signInWithOAuth` + `/auth/callback`)
- [x] Navbar Login / Logout / 프로필 링크
- [x] 세션 유지 (`proxy.ts`)
- [x] `profiles` 테이블 + RLS (`001_profiles.sql`)
- [x] `/profile` 프로필 페이지

### Phase 2 — 계정 연동 ✅
- [x] **2-1 ~ 2-4** Riot / Discord / 전적(코드) 연동
- [x] **2-5** Riot / Discord 연동 해제 (`017_account_unlink.sql`)
- [x] **2-6** Valorant 전적 불러오기 (Production Key + `/api/valorant/sync`)
  - [x] 라이엇 연동 직후 자동 전적 동기화
  - [x] 프로필 「전적 불러오기」 버튼 → 최근 경기 목록 즉시 갱신
  - [x] 5분 쿨다운 + 에러 메시지 (한/영)
  - [x] Production Key 스모크 테스트 (`scripts/check-riot-key.mjs` → OK)
  - [x] 보안 패치: puuid 소유권 검증, 실패 시에도 쿨다운, link/sync 에러 메시지 살균, 전역 rate limit
  - [x] 한국 서버 샤드 수정 (`kr` 자동 감지 + `ap` 폴백) — 잘못된 `ap` 호출로 400/쿨다운 나던 문제 해결

### Phase 4 — 실시간 매칭 ✅
- [x] **4-1** 온라인 상태 (`007_online_presence.sql`)
- [x] **4-2** 매칭 큐 (`008_match_queue.sql`)
- [x] **4-3** 매칭 알고리즘 (`009_duo_matching.sql`)
- [x] **4-4** 파티 링크 + Discord 연결 UI (`010_match_connection.sql`)
  - [x] 보이스 선택 (발로란트 / Discord / No Voice)
  - [x] No Voice 불일치 시 취소 안내 + 상대 응답 대기 UI
  - [x] 파티 코드 + Discord 프로필 연결
  - [x] 보안 패치 (Discord ID 조건부 노출, 파티 코드 덮어쓰기 차단)
- [x] **4-5** 매칭 무응답 자동 취소 (`011_match_timeout.sql`)
  - [x] 상대 보이스 선택 항상 표시
  - [x] 90초 무응답 시 자동 취소 + 안내 메시지
- [x] **4-6** 매칭 중 이탈 처리 (`012_match_partner_offline.sql`)
  - [x] 상대 탭 닫기/이탈 시 남은 사람 → 기본 화면 + 「상대가 나갔습니다」
  - [x] 나갔다가 돌아온 사람 → 기본 화면 + 「매칭이 취소되었습니다」
- [x] **4-7** 파티 구성 완료/취소 + 게임 중 화면 (`013_match_setup_phase.sql`)
  - [x] 보이스 선택 후 완료/취소 버튼, 4분 타임아웃
  - [x] 양쪽 완료 → 「게임 중입니다」화면
- [x] **4-8** 매칭 전 참고사항 모달 (첫 이용 필수 + 1주일 다시 보지 않기)
- [x] **4-9** 매칭 후 리뷰 + 신뢰도/매너등급 (`015_match_reviews.sql`)
  - [x] 게임 종료 후 상대 평가 모달 (긍정/부정 태그, 나중에 하기 없음 — 필수)
  - [x] 신뢰도 0~100 + 매너등급 S/A/B/C/D
  - [x] review_count < 3 → 🆕 신규 유저 뱃지
  - [x] 매칭 카드·프로필에 등급/신뢰도/태그 노출
  - [x] RLS + RPC (참여한 match_id의 상대에게만 리뷰)
- [x] **4-10** 이탈 페널티 시스템 (`016_penalty_system.sql`)
  - [x] 매칭 잡혔는데 이탈 시 1~3회 경고 / 4회 5분 쿨다운 / 5회+ 15분 쿨다운
  - [x] 이탈마다 신뢰도 -8 감점
  - [x] 프로필 페이지에서 내 페널티 기록 전체 조회
  - [x] 쿨다운 중 큐 가입 시도 → 해제 시각 표시하며 차단
- [x] **4-11** No Voice 수락/거절 UX (`018_no_voice_flow.sql`)
  - [x] 상대 no voice 선택 시 「아니요, 괜찮습니다」 / 「매칭 취소」 버튼
  - [x] no voice 거절 시 페널티 없음
  - [x] no voice 고른 사람은 상대 수락 전 파티 구성 완료 버튼 숨김
- [x] **4-12** Valorant 서버별 매칭 (`019_valorant_shard_matching.sql`)
  - [x] Riot active-shard API로 `kr` / `ap` 등 실제 플레이 서버 저장
  - [x] 라이엇 연동·전적 동기화·큐 입장 시 `valorant_shard` 자동 저장/복구
  - [x] 같은 shard 유저끼리만 매칭, 다른 shard 큐는 서로 막지 않음
  - [x] 보안 패치: 진행 중(`active`/`in_game`) 매치 유저 큐 재입장 차단, active-shard 조회 전역 rate limit, 라이엇 해제 시 shard 제거
- [x] **보안 패치** — CSRF(Origin) 검증, UUID 검증, expire RPC 스로틀, 큐 중복 차단, 보안 헤더
- [x] **보안 점검 (7/12)** — 전적 sync 쿨다운 우회 차단(최소 60초), 실패 시에도 타임스탬프 기록, OAuth 에러 문구 살균, 디버그 스크립트 git 제외

---

## 보안 상태 (2026-07-06)

| 항목 | 상태 |
|------|------|
| `.env` git 제외 | ✅ |
| service_role 서버 전용 | ✅ |
| RLS + RPC 직접 쓰기 차단 | ✅ |
| Open redirect 방지 | ✅ |
| POST Origin/CSRF 검증 | ✅ (패치) |
| profiles_match_public anon 차단 | ✅ (024) |
| tier/shard 서버 RPC 전용 | ✅ (024) |
| save_playstyle_analysis 입력 검증 | ✅ (024) |
| presence/sync 타임스탬프 RPC 전용 | ✅ (025) |
| sync_valorant_matches 입력 제한 | ✅ (025) |
| reputation/trust_score RPC 전용 | ✅ (026) |
| matchId UUID 검증 | ✅ (패치) |
| Riot OAuth 소유 증명 | 🟡 Production Key/RSO 대기 |
| 분산 rate limit (Upstash) | 🟡 Vercel 인메모리 한계 |

### Phase 5 — 배포 ✅
- [x] `npm run build` 프로덕션 빌드 성공
- [x] `GET /api/health` smoke test
- [x] GitHub push + Vercel Import + 환경변수 설정
- [x] Supabase Redirect URL (배포 URL) 설정
- [x] 배포 URL에서 health / 로그인 / 매칭 확인 완료

---

## 유료 플랜 출시 때 💎

- [x] AI API 키 로컬 등록 (`.env.local` — `ANTHROPIC_API_KEY`, `GEMINI_API_KEY`)
- [x] `020_ai_playstyle_profile.sql` — profiles AI/플랜 컬럼 + RLS + 매칭 공개 뷰
- [x] `lib/analysis/gemini.ts` — 무료 플랜용 Gemini 2.5 Flash 플레이스타일 분석 함수 (실패 시 기본값 폴백)
- [x] `POST /api/ai/analyze` — 저장된 전적 집계 → Gemini 분석 → `save_playstyle_analysis` RPC 저장 (1시간 쿨다운 + rate limit)
- [x] `lib/analysis/claude.ts` — 프리미엄용 Claude Sonnet 심층 분석 함수 (크레딧 없어도 더미 폴백으로 플로우 테스트 가능)
- [x] `lib/analysis/scheduler.ts` — 재분석 스케줄러 (free=큐마다 규칙 / premium=3일 Claude / 최초 즉시)
- [x] `lib/analysis/aggregate.ts` — 전적 집계 공용 모듈 (분석 API와 스케줄러가 공유)
- [x] `021_synergy_notes.sql` — `profiles.synergy_notes` 컬럼 + `save_playstyle_analysis` RPC 6번째 파라미터 추가
- [x] `scheduler.ts` — Claude `synergy_notes` RPC에 연결 (Gemini는 자동 생략)
- [x] `queue/join` route — 큐 입장 시 `runAnalysis` non-blocking 백그라운드 실행
- [x] `lib/matching/matcher.ts` — 궁합 매칭 순수 로직 (하드/소프트 조건 + 시간 완화, `findBestMatch` + `calculateSynergyScore`)
- [x] `022_tier_and_smart_match.sql` — `profiles.tier` 컬럼 + `get_match_queue_candidates` / `create_duo_match` RPC
- [x] `lib/matching/runMatch.ts` — 후보 조회 → `findBestMatch` → `create_duo_match` (A안: 계산은 TS, DB는 조회+원자적 생성)
- [x] `POST /api/matching/queue` — STEP 6 큐 입장 + findBestMatch 매칭 API (`handleQueuePost` 공용 핸들러)
- [x] `/api/match/queue/join` — 기존 경로 호환 (동일 핸들러 재사용)
- [ ] Supabase SQL Editor에서 `022_tier_and_smart_match.sql` 실행
- [x] `lib/riot/rank.ts` + `lib/riot/syncTier.ts` — Riot competitiveupdates API → `profiles.tier` (Iron1=0 … Radiant=26)
- [x] 티어 동기화 — 라이엇 연동 / 전적 sync / 큐 입장 시 자동 갱신 (티어 null이면 즉시, 이후 1시간 rate limit)
- [x] `lib/analysis/executePlanAnalysis.ts` — free=규칙기반 / premium=규칙기반+Claude 4축
- [x] `/api/ai/analyze` — 플랜별 분기 (rule_based | claude)
- [x] `lib/constants/matching-thresholds.ts` — 공격성 가중치·ACS baseline·태그 임계값 상수 분리
- [x] `lib/analysis/ruleBasedAnalyzer.ts` — 무료 정량 분석 (LLM 없음, role별 ACS 정규화)
- [x] `lib/analysis/claude.ts` — 정량 재사용 + trend/situational/anomaly/synergy 4축 (MAX_TOKENS=2000)
- [x] `023_rule_based_and_claude_notes.sql` — 4축 컬럼 + analysis_source `rule_based` + RPC 확장
- [x] Gemini 호출 제거(파일·키는 롤백용 유지)
- [x] `024_security_hardening.sql` — 뷰 anon/public 차단, tier/shard RPC 전용, 분석 RPC 입력 검증
- [x] `lib/supabase/profileServerWrites.ts` — set_valorant_shard / set_profile_tier RPC 래퍼
- [ ] Supabase SQL Editor에서 `023_rule_based_and_claude_notes.sql` 실행
- [ ] Supabase SQL Editor에서 `024_security_hardening.sql` 실행
- [ ] Supabase SQL Editor에서 `025_security_presence_sync.sql` 실행
- [ ] Supabase SQL Editor에서 `026_security_reputation.sql` 실행
- [ ] Supabase SQL Editor에서 `020_ai_playstyle_profile.sql` 실행 (⚠️ 이거 안 하면 분석 저장 실패)
- [ ] 프로필 페이지에 「AI 분석」 버튼 + 태그 표시 UI
- [ ] Vercel에 AI API 키 등록 (Dashboard → Environment Variables, Sensitive 체크)

---

## MVP 이후 (Riot Production Key)

- [x] 이용약관 `/terms` + 개인정보처리방침 `/privacy` (footer `Link`로 페이지 이동)
- [x] Production API Key 신청 → `riot.txt` 도메인 검증 **Verified 완료**
- [x] Production API Key → Valorant 전적 실제 동작 (로컬 `.env.local` + Vercel `RIOT_API_KEY`)
- [ ] Riot OAuth (RSO) — OP.GG 스타일 계정 연동
- [x] **문의 이메일** — `site.contact.ts` → leedoo101009@gmail.com

---

## 다음에 할 일 (선택)

- [ ] 프로필 → 라이엇 연동 → 「전적 불러오기」 실제 테스트 (최근 경기 있는 계정)
- [ ] Supabase SQL Editor에서 **`005_valorant_matches.sql`** 실행 (전적 테이블, 아직 미실행 시)
- [ ] Supabase SQL Editor에서 **`015_match_reviews.sql`** 실행 (리뷰 기능, 아직 미실행 시)
- [ ] Supabase SQL Editor에서 **`016_penalty_system.sql`** 실행 (페널티 기능, 아직 미실행 시)
- [ ] Supabase SQL Editor에서 **`017_account_unlink.sql`** 실행 (연동 해제)
- [ ] Supabase SQL Editor에서 **`018_no_voice_flow.sql`** 실행 (No Voice UX)
- [ ] Supabase SQL Editor에서 **`019_valorant_shard_matching.sql`** 실행 (Valorant 서버별 매칭)
- [ ] Supabase SQL Editor에서 `011` ~ `014` migration 실행 (아직 안 했다면)
- [ ] 친구한테 배포 URL 공유해서 실제 2인 매칭 테스트
- [ ] Riot Production Key **승인 대기** (이메일/포털 확인)
- [ ] Supabase SQL Editor에서 **`020_ai_playstyle_profile.sql`** 실행 (AI 프로필 컬럼)
- [ ] Supabase SQL Editor에서 **`021_synergy_notes.sql`** 실행 (Claude 시너지 노트)
- [ ] Supabase SQL Editor에서 **`022_tier_and_smart_match.sql`** 실행 (tier + 스마트 매칭)
- [x] STEP 7 환경변수 점검 — 로컬·Vercel 모두 필수 키 등록 확인, 서버 전용 사용 검증 완료
- [x] 보안·버그 점검 (7/13) — Riot 키 Production 확인(`PRODUCTION_KEY_LIKELY_OK`), status 폴링 매칭을 큐 참여자로 제한, 즉시 매칭 시 UI 상태 오류 수정
- [x] `server-only` 패키지 추가 — AI/Riot/admin 모듈에 빌드 타임 서버 전용 가드
- [x] AI 분석 — free=규칙기반 / premium=Claude 4축 + Riot 티어 매칭 연동
- [x] 보안 점검 (7/18) — 024 migration, tier/shard RPC, 분석 RPC 검증, Claude JSON 파싱 방어
- [x] 보안 재점검 (7/18) — 025 presence/sync RPC, sendBeacon CSRF, redirect·헤더·리뷰 태그 제한
- [x] `026_security_reputation.sql` — trust_score/review/penalty/cooldown RPC+트리거
- [x] AI 재분석 lazy refresh — 큐 입장 직전 조건부 실행 (백그라운드/cron 일괄 제거)
- [x] 프로필 UI — Valorant 티어 + RR 표시 (`027_ranked_rating.sql`)
- [x] premium 매칭 차별화 — Claude `match_prefs` → 매칭 점수 반영 (free↔premium 동일 풀)
- [x] free 재분석 — 큐 입장마다 규칙기반 갱신 (7일 캐시 제거), premium은 3일 Claude 유지
- [x] 플랜 차별화 1차 마무리 (매칭 prefs + free 매번 규칙 / premium 3일 Claude) — UI·결제 문구는 보류
- [ ] Supabase SQL Editor에서 `027_ranked_rating.sql` 실행 (아직이면)
- [ ] Supabase SQL Editor에서 **`028_premium_match_prefs.sql`** 실행 (아직이면)

---

## 현재 기술 스택 상태

| 항목 | 상태 |
|------|------|
| Supabase Auth | ✅ |
| Riot / Discord 연동 | ✅ |
| Valorant 전적 | ✅ Production Key 연동 |
| 실시간 매칭 | ✅ + premium match_prefs 순위 가산 |
| Vercel 배포 | ✅ **완료** |
| Claude AI | ✅ premium 4축 노트 + match_prefs (정량은 rule_based) |
| Gemini AI | 🟡 롤백용 보관 (현재 free는 미사용) |
| 규칙기반 분석 | ✅ free 정량 (aggression/tags/role, prefs 없음) |
| 티어 매칭 | ✅ Riot API → profiles.tier (±3단계, 언랭은 조건 생략) |
